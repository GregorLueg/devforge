# The `\itemize{}` entry for a merged field

The `\itemize{}` entry for a merged field

## Usage

``` r
merge_item_prose(field, name)
```

## Arguments

- field:

  A `devforge_field` of type `"merge"`.

- name:

  String. The field name.

## Value

String. One sentence run, unwrapped.
