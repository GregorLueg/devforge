# Run air over a set of files

Run air over a set of files

## Usage

``` r
run_air(paths)
```

## Arguments

- paths:

  Character vector of file paths.

## Value

`TRUE` if air ran, `FALSE` if it is not on the path.
