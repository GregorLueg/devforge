# The names of a spec's returned list, in order

The names of a spec's returned list, in order

## Usage

``` r
spec_field_names(spec)
```

## Arguments

- spec:

  A `devforge_spec`.

## Value

Character vector of field names.
