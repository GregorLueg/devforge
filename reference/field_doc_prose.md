# The roxygen prose for one field

Type annotation, the spec's prose, the allowed choices where there are
any, and the default. The last two are derived rather than written by
hand, which is the point.

## Usage

``` r
field_doc_prose(field)
```

## Arguments

- field:

  A `devforge_field`.

## Value

String. One sentence run, unwrapped.
