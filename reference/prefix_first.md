# Put a prefix in front of the first line only

Put a prefix in front of the first line only

## Usage

``` r
prefix_first(prefix, lines)
```

## Arguments

- prefix:

  String. What goes in front.

- lines:

  Character vector. The source lines.

## Value

Character vector with `prefix` on the first line.
