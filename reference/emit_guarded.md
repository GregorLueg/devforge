# A guarded call that returns early on failure

A guarded call that returns early on failure

## Usage

``` r
emit_guarded(call_lines)
```

## Arguments

- call_lines:

  Character vector. The source of the call.

## Value

Character vector of R source lines.
