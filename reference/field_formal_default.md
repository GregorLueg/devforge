# The source text of a field's formal default

The source text of a field's formal default

## Usage

``` r
field_formal_default(field)
```

## Arguments

- field:

  A `devforge_field`.

## Value

String. R source for the right hand side of the formal.
