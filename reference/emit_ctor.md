# A generated `params_*()` constructor

A generated `params_*()` constructor

## Usage

``` r
emit_ctor(spec)
```

## Arguments

- spec:

  A `devforge_spec`.

## Value

Character vector of R source lines, roxygen included.
