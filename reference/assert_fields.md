# Validate a named list of fields

Validate a named list of fields

## Usage

``` r
assert_fields(fields)
```

## Arguments

- fields:

  Named list of `devforge_field` objects.

## Value

`TRUE`, invisibly. Errors otherwise.
