% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fields.R
\name{field_qassert}
\alias{field_qassert}
\title{Derive the qassert pattern for a field}
\usage{
field_qassert(field, for_check = FALSE)
}
\arguments{
\item{field}{A \code{devforge_field}.}

\item{for_check}{Boolean. Return the checker's pattern, which \code{check_as}
may override, rather than the constructor's. Defaults to \code{FALSE}.}
}
\value{
Character vector of qassert patterns, or \code{NULL} for a \code{"free"}
field, which carries no automatic validation.
}
\description{
Folds type, length, range and nullability into the single
pattern that both the constructor's \code{\link[checkmate:qassert]{checkmate::qassert()}} call and the
checker's rule table use. This is what settles the two competing spellings
of "integer or NULL" found across the packages onto one.
}
\keyword{internal}
