% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fields.R
\name{p_merge}
\alias{p_merge}
\title{Merged sub-list field}
\usage{
p_merge(from, default = list(), overrides = NULL, doc = NULL)
}
\arguments{
\item{from}{String or language object. The name of a spec in the same
package (\code{"knn_defaults"} merges into \code{params_knn_defaults()}), or an
expression evaluated inside the constructor, for a base that depends on the
other arguments. A language \code{from} contributes nothing to the checker.}

\item{default}{List or language object. The formal default. Defaults to
\code{list()}.}

\item{overrides}{List, language object or \code{NULL}. Constructor-specific
defaults applied over \code{from} before the caller's list. A language object may
reference the other formals, e.g.
\code{quote(list(k = neighbours_within_batch * 2L))}. Defaults to \code{NULL}.}

\item{doc}{String or \code{NULL}. Roxygen prose for this field.}
}
\value{
A \code{devforge_field}.
}
\description{
For the constructors that take a list of overrides for a
shared defaults block, e.g. \code{knn = list(k = 25L)} on top of
\code{params_knn_defaults()}. The constructor merges the caller's list into the
base with \code{modifyList(keep.null = TRUE)} and splices the result flat into
the returned list at this field's position. When \code{from} names another spec,
the checker requires the spliced elements and validates them with that
spec's rules.
}
