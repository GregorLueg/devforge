% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{param_spec}
\alias{param_spec}
\title{Parameter wrapper specification}
\usage{
param_spec(
  name,
  title,
  fields,
  description = NULL,
  details = NULL,
  return_order = NULL,
  checker = to_pascal_case(name),
  checker_args = list(),
  extra_ctor = NULL,
  extra_check = NULL,
  class_tag = NULL,
  strict_names = FALSE,
  label = NULL,
  hint = NULL,
  test_fn = FALSE,
  export = TRUE
)
}
\arguments{
\item{name}{String. Snake case stem. \code{"ica_general"} gives
\code{params_ica_general()}.}

\item{title}{String. The roxygen title line for the constructor.}

\item{fields}{Named list of \code{\link[=p_int]{p_int()}} and friends. Order is the order of the
formals and of the returned list.}

\item{description}{String or \code{NULL}. Roxygen \verb{@description} prose. Defaults
to \code{NULL}.}

\item{details}{String or \code{NULL}. Roxygen \verb{@details} prose, emitted verbatim
so it can carry its own \verb{\\itemize\{\}}. Defaults to \code{NULL}.}

\item{return_order}{Character vector or \code{NULL}. The order of the returned
list, when it differs from the order of the formals. Must be a permutation
of \code{names(fields)}. Defaults to \code{NULL}, meaning the order of the formals.}

\item{checker}{String or \code{NULL}. PascalCase stem for the checkmate
extension, giving \verb{check<stem>Params()} and \verb{assert<stem>Params()}. Two
specs may share a stem, in which case the checker is emitted once and the
field sets must agree. \code{NULL} emits no checker. Defaults to the PascalCase
form of \code{name}.}

\item{checker_args}{A list made with \code{\link[base:alist]{base::alist()}}. Extra formals for the
checker beyond \code{x}, for the checkers that cross-reference an outside object.
Defaults to an empty list.}

\item{extra_ctor}{Language object or \code{NULL}. Emitted verbatim into the
constructor after the per-field assertions. For cross-field rules.}

\item{extra_check}{Language object or \code{NULL}. Emitted verbatim into the
checker before its final return. Must \code{return()} an error string on failure.}

\item{class_tag}{String or \code{NULL}. When set, the returned list is tagged
\code{c(class_tag, "list")}. Only for the params objects that are dispatched on
with \code{inherits()}. Defaults to \code{NULL}.}

\item{strict_names}{Boolean. \code{TRUE} demands exactly the declared names,
\code{FALSE} uses \code{must.include} and tolerates extras. Defaults to \code{FALSE}.}

\item{label}{String or \code{NULL}. Human readable label used in the checker's
error messages. Defaults to \code{name} with underscores replaced by spaces,
followed by \code{"params"}.}

\item{hint}{String or \code{NULL}. Appended to the checker's error messages to
describe the expected types and ranges.}

\item{test_fn}{Boolean. Also emit \verb{test<stem>Params()} via
\code{\link[checkmate:makeTestFunction]{checkmate::makeTestFunction()}}. Defaults to \code{FALSE}.}

\item{export}{Boolean. Whether the constructor gets \verb{@export}. Defaults to
\code{TRUE}.}
}
\value{
A list of class \code{devforge_spec}.
}
\description{
The declarative description of one \verb{params_*()} constructor and
its paired checkmate extension. Everything the generator emits comes from
here.
}
