% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fields.R
\name{new_field}
\alias{new_field}
\title{Build a field object}
\usage{
new_field(
  type,
  default,
  range = NULL,
  null_ok = FALSE,
  len = 1L,
  choices = NULL,
  letter = NULL,
  check_as = NULL,
  doc = NULL
)
}
\arguments{
\item{type}{String. One of \code{c("int", "dbl", "lgl", "chr", "choice", "free")}.}

\item{default}{Any. The default value for the formal.}

\item{range}{String or \code{NULL}. A checkmate range suffix such as \code{"[1,)"}.}

\item{null_ok}{Boolean. Whether \code{NULL} is a permitted value.}

\item{len}{Integer or string. Length token for the qassert code. \code{1L} for a
scalar, \code{"+"} for one or more, \code{"*"} for any length.}

\item{choices}{Character vector or \code{NULL}. Allowed values for \code{"choice"}.}

\item{letter}{String or \code{NULL}. Overrides the qassert base letter, for the
rare case where a stricter class is wanted (\code{"R"} instead of \code{"N"}).}

\item{check_as}{Character vector or \code{NULL}. Overrides the pattern used in
the checker's rule table. For fields the constructor resolves, such as a
worker count that takes \code{NULL} and comes back as an integer.}

\item{doc}{String or \code{NULL}. Roxygen prose for this field.}
}
\value{
A list of class \code{devforge_field}.
}
\description{
Internal constructor behind \code{\link[=p_int]{p_int()}} and friends. A field is
the smallest unit of a spec: everything needed to emit one formal, one
assertion, one rule table entry and one roxygen line.
}
\keyword{internal}
