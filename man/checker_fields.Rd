% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emit_check.R
\name{checker_fields}
\alias{checker_fields}
\title{The fields a spec's checker validates, merged fields expanded}
\usage{
checker_fields(spec, specs = list())
}
\arguments{
\item{spec}{A \code{devforge_spec}.}

\item{specs}{Named list of \code{devforge_spec} objects that merged fields may
refer to. Defaults to an empty list.}
}
\value{
Named list of \code{devforge_field} objects in return order.
}
\description{
A merged field that names another spec is replaced by that
spec's own checker fields, recursively. A merged field with a language
\code{from} contributes nothing. When a name turns up twice the first one wins,
matching what \code{$} finds on the list that \code{c()} builds.
}
\keyword{internal}
