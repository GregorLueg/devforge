% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{stable_number_src}
\alias{stable_number_src}
\title{Source for a numeric default that survives the trip through roxygen}
\usage{
stable_number_src(x, src)
}
\arguments{
\item{x}{The default value.}

\item{src}{String. The literal \code{\link[=deparse_value]{deparse_value()}} produced for it.}
}
\value{
String. R source for the right hand side of the formal.
}
\description{
roxygen writes the Rd \verb{\\usage} by deparsing the default it
parsed out of the generated source, and \verb{R CMD check}'s codoc then compares
the two renderings. For nearly every value \code{deparse()} is a fixed point and
the literal can stand as it is. It is not one for a value like \code{1e-300},
where 15 significant digits are too few: \code{deparse()} gives
\code{9.99999999999999e-301}, deparsing that again gives
\code{9.99999998481683e-301}, and codoc reports a mismatch. There the literal is
wrapped in \code{as.numeric()}, which keeps the exact value and leaves roxygen a
call to print verbatim.
}
\keyword{internal}
